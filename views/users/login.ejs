<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>SeatWorld | Log in</title>

  <style>
    * {
      box-sizing: border-box;
    }

    html,
    body {
      overflow-x: hidden;
      width: 100%;
      max-width: 100%;
    }

    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background: #fff;
      display: flex;
      flex-direction: column;
      align-items: center;
      min-height: 100vh;
    }

    /* Header */
    header {
      width: 100%;
      padding: 15px 20px;
      border-bottom: 1px solid #eaeaea;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    header h1 {
      margin: 0;
      font-size: 22px;
      font-weight: bold;
      color: #0b64c0;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    header img {
      width: 28px;
      height: 28px;
    }

    header a {
      text-decoration: none;
      font-size: 14px;
      color: #333;
    }

    /* Login Container */
    .login-container {
      margin-top: 50px;
      text-align: center;
      padding: 0 15px;
      width: 100%;
      flex: 1;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .login-title {
      font-size: 24px;
      margin-bottom: 30px;
      color: #333;
    }

    /* Form Box */
    .form-box {
      display: flex;
      justify-content: center;
      width: 100%;
    }

    /* Login Form */
    .login-form {
      display: flex;
      flex-direction: column;
      gap: 15px;
      width: 100%;
      max-width: 280px;
      /* mobile default */
      background: #fafafa;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.05);
      text-align: left;
    }

    .login-form label {
      font-size: 14px;
      color: #333;
    }

    .login-form input {
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 6px;
      font-size: 14px;
      width: 100%;
    }

    .login-form button {
      padding: 12px;
      background: #0b64c0;
      border: none;
      border-radius: 20px;
      cursor: pointer;
      font-size: 14px;
      color: #fff;
    }

    .login-form button:hover {
      background: #084b91;
    }

    /* Messages */
    .error-message {
      background: #fee;
      border: 1px solid #fcc;
      color: #c33;
      padding: 12px;
      border-radius: 8px;
      font-size: 14px;
      text-align: center;
    }

    .success-message {
      background: #efe;
      border: 1px solid #cfc;
      color: #3c3;
      padding: 12px;
      border-radius: 8px;
      font-size: 14px;
      text-align: center;
    }

    /* Links */
    .links {
      font-size: 13px;
      text-align: center;
    }

    .links a {
      color: #0b64c0;
      text-decoration: none;
    }

    .links a:hover {
      text-decoration: underline;
    }

    /* Divider */
    .divider {
      display: flex;
      align-items: center;
      margin: 15px 0;
    }

    .divider-line {
      flex: 1;
      height: 1px;
      background: #ddd;
    }

    /* Google Button */
    .google-btn {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      border: 1px solid #ccc;
      padding: 10px;
      border-radius: 20px;
      text-decoration: none;
      color: #000;
      font-size: 14px;
    }

    .google-btn img {
      width: 20px;
    }

    .google-btn:hover {
      background: #f7f7f7;
    }

    /* 🔹 DESKTOP ONLY – wider inputs */
    @media (min-width: 1024px) {
      .login-form {
        max-width: 380px;
      }

      .login-form input {
        padding: 12px;
        font-size: 15px;
      }
    }

    /* Small Screens */
    @media (max-width: 480px) {
      header h1 {
        font-size: 18px;
      }

      .login-title {
        font-size: 18px;
      }
    }

    .auth-error {
      background-color: #ffe5e5;
      color: #b00020;
      border: 1px solid #000000;
      padding: 12px 16px;
      border-radius: 8px;
      font-size: 14px;
      text-align: center;
      margin-bottom: 16px;
    }
  </style>
</head>

<body>
  <%- include('../partials/flash') %>

    <header>
      <h1>
        <img src="https://res.cloudinary.com/dbdnmlzy6/image/upload/v1765945540/products/rmsn314w038gnfzmemmi.jpg" />
        SeatWorld
      </h1>
      <a href="/">← Back</a>
    </header>

    <div class="login-container">
      <div class="login-title">Log in</div>

      <div class="form-box">
        <form class="login-form" method="POST" action="/login">




          <label>Email address</label>
          <input type="email" name="email" required>

          <label>Password</label>
          <input type="password" name="password" required>

          <button type="submit">Log in</button>

          <div class="links">
            <a href="/forgot-password">Forgot password?</a>
          </div>

          <div class="links">
            Don’t have an account? <a href="/signup">Register</a>
          </div>

          <div class="divider">
            <div class="divider-line"></div>
            <span style="padding:0 10px;">OR</span>
            <div class="divider-line"></div>
          </div>

          <a href="/auth/google" class="google-btn">
            <img src="https://www.svgrepo.com/show/355037/google.svg">
            Continue with Google
          </a>

        </form>
      </div>
    </div>

</body>

</html>