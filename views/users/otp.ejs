<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>OTP Verification - SeatWorld</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/users/otp.css">
</head>

<body>
  <%- include('../partials/authHeader') %>

    <!-- OTP Card -->
    <div class="container">
      <div class="card">
        <img src="/images/mail-icon.png" alt="Mail Icon">
        <p>Please check your inbox<br>We’ve sent a code to your email</p>

        <!-- ✅ Show messages -->

        <form action="/verify-otp" method="POST">
          <div class="otp-inputs">
            <input type="number" name="otp1" maxlength="1" required class="otp-input">
            <input type="number" name="otp2" maxlength="1" required class="otp-input">
            <input type="number" name="otp3" maxlength="1" required class="otp-input">
            <input type="number" name="otp4" maxlength="1" required class="otp-input">
          </div>

          <div class="timer" id="timer"></div>
          <div class="buttons">
            <button type="button" class="cancel" onclick="window.location='/login'">Cancel</button>
            <button type="submit" class="verify">Verify</button>
          </div>
        </form>

        <div class="resend" id="resendBox">
          Didn't receive code?
          <a href="/resend-otp?type=signup">Click to resend</a>
        </div>
      </div>
    </div>

    <input type="hidden" id="otpExpiryValue"
      value="<%= (typeof otpExpires !== 'undefined' && otpExpires) ? new Date(otpExpires).getTime() : 0 %>">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script type="module" src="/js/users/otp.js"></script>



</body>

</html>