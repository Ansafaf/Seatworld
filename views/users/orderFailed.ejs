<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Failed | SeatWorld</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
    <style>
       /* ===== ORDER FAILED PAGE STYLES ===== */

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
    background: linear-gradient(135deg, #fff5f5 0%, #ffe8e8 100%);
    margin: 0;
    padding: 0;
    min-height: 100vh;
}

/* ===== FAILURE CONTAINER ===== */
.failure-container {
    min-height: 70vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 60px 20px;
    position: relative;
}

/* Decorative background elements */
.failure-container::before {
    content: '';
    position: absolute;
    top: 10%;
    right: 10%;
    width: 300px;
    height: 300px;
    background: radial-gradient(circle, rgba(220, 53, 69, 0.08) 0%, transparent 70%);
    border-radius: 50%;
    pointer-events: none;
}

.failure-container::after {
    content: '';
    position: absolute;
    bottom: 15%;
    left: 5%;
    width: 250px;
    height: 250px;
    background: radial-gradient(circle, rgba(211, 47, 47, 0.06) 0%, transparent 70%);
    border-radius: 50%;
    pointer-events: none;
}

/* ===== FAILURE CARD ===== */
.failure-card {
    background: #ffffff;
    border-radius: 20px;
    box-shadow: 
        0 10px 40px rgba(0, 0, 0, 0.1),
        0 0 0 1px rgba(0, 0, 0, 0.02);
    padding: 60px 80px;
    text-align: center;
    max-width: 680px;
    width: 100%;
    position: relative;
    animation: fadeInUp 0.6s ease-out;
    overflow: hidden;
}

@keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(40px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

/* Decorative gradient border effect */
.failure-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 5px;
    background: linear-gradient(90deg, #dc3545 0%, #ff6b6b 100%);
}

/* ===== FAILURE ICON ===== */
.failure-icon {
    width: 100px;
    height: 100px;
    background: linear-gradient(135deg, #dc3545 0%, #c82333 100%);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 32px;
    box-shadow: 
        0 8px 24px rgba(220, 53, 69, 0.3),
        0 0 0 12px rgba(220, 53, 69, 0.08);
    animation: shakeIn 0.6s ease-out;
    position: relative;
}

@keyframes shakeIn {
    0% {
        transform: scale(0) rotate(-180deg);
        opacity: 0;
    }
    50% {
        transform: scale(1.1) rotate(10deg);
    }
    75% {
        transform: scale(0.95) rotate(-5deg);
    }
    100% {
        transform: scale(1) rotate(0deg);
        opacity: 1;
    }
}

/* Pulsing warning effect */
.failure-icon::after {
    content: '';
    position: absolute;
    width: 100%;
    height: 100%;
    border-radius: 50%;
    border: 3px solid #dc3545;
    animation: pulse 2s ease-out infinite;
}

@keyframes pulse {
    0% {
        transform: scale(1);
        opacity: 1;
    }
    100% {
        transform: scale(1.5);
        opacity: 0;
    }
}

.failure-icon i {
    font-size: 48px;
    color: #ffffff;
    animation: iconPop 0.4s ease-out 0.3s both;
}

@keyframes iconPop {
    0% {
        transform: scale(0);
    }
    50% {
        transform: scale(1.2);
    }
    100% {
        transform: scale(1);
    }
}

/* ===== TEXT CONTENT ===== */
.failure-title {
    font-size: 2.5rem;
    font-weight: 700;
    color: #1a1a1a;
    margin-bottom: 16px;
    animation: fadeIn 0.6s ease-out 0.4s both;
    letter-spacing: -0.5px;
}

@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(10px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.failure-message {
    font-size: 1.05rem;
    color: #666;
    line-height: 1.8;
    margin-bottom: 44px;
    max-width: 480px;
    margin-left: auto;
    margin-right: auto;
    animation: fadeIn 0.6s ease-out 0.6s both;
}

.order-id {
    display: inline-block;
    color: #d32f2f;
    font-weight: 700;
    font-family: 'Courier New', monospace;
    background: #ffebee;
    padding: 4px 12px;
    border-radius: 6px;
    margin-top: 8px;
    font-size: 0.95rem;
    letter-spacing: 0.5px;
}

/* ===== BUTTON GROUP ===== */
.button-group {
    display: flex;
    gap: 16px;
    justify-content: center;
    flex-wrap: wrap;
    animation: fadeIn 0.6s ease-out 0.8s both;
}

.btn {
    padding: 16px 36px;
    border-radius: 12px;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    border: none;
    letter-spacing: 0.3px;
    position: relative;
    overflow: hidden;
}

.btn::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 0;
    height: 0;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.2);
    transform: translate(-50%, -50%);
    transition: width 0.6s, height 0.6s;
}

.btn:hover::before {
    width: 300px;
    height: 300px;
}

/* Primary Button - Try Again */
.btn-primary {
    background: linear-gradient(135deg, #dc3545 0%, #c82333 100%);
    color: #ffffff;
    box-shadow: 0 4px 14px rgba(220, 53, 69, 0.3);
}

.btn-primary:hover {
    background: linear-gradient(135deg, #c82333 0%, #bd2130 100%);
    transform: translateY(-3px);
    box-shadow: 0 8px 24px rgba(220, 53, 69, 0.4);
}

.btn-primary:active {
    transform: translateY(-1px);
}

/* Secondary Button - Go to Homepage */
.btn-secondary {
    background: #ffffff;
    color: #333;
    border: 2px solid #333;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
}

.btn-secondary:hover {
    background: #f5f5f5;
    transform: translateY(-3px);
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.12);
    border-color: #000;
}

.btn-secondary:active {
    transform: translateY(-1px);
}

/* ===== HELP SECTION ===== */
.help-section {
    margin-top: 40px;
    padding-top: 32px;
    border-top: 2px dashed #e0e0e0;
}

.help-title {
    font-size: 1rem;
    font-weight: 600;
    color: #333;
    margin-bottom: 16px;
}

.help-options {
    display: flex;
    justify-content: center;
    gap: 32px;
    flex-wrap: wrap;
}

.help-item {
    text-align: center;
    flex: 1;
    min-width: 140px;
    max-width: 180px;
}

.help-icon {
    font-size: 28px;
    color: #dc3545;
    margin-bottom: 8px;
}

.help-label {
    font-size: 0.85rem;
    color: #666;
    margin-bottom: 4px;
}

.help-link {
    font-size: 0.9rem;
    font-weight: 600;
    color: #dc3545;
    text-decoration: none;
    transition: color 0.3s ease;
}

.help-link:hover {
    color: #c82333;
    text-decoration: underline;
}

/* ===== INFO BOX ===== */
.info-box {
    background: #fff8e1;
    border-left: 4px solid #ffc107;
    padding: 16px 20px;
    border-radius: 8px;
    margin-top: 32px;
    text-align: left;
}

.info-box-title {
    font-size: 0.9rem;
    font-weight: 700;
    color: #f57c00;
    margin-bottom: 8px;
    display: flex;
    align-items: center;
    gap: 8px;
}

.info-box-content {
    font-size: 0.85rem;
    color: #666;
    line-height: 1.6;
}

.info-box-content ul {
    margin: 8px 0;
    padding-left: 20px;
}

.info-box-content li {
    margin: 4px 0;
}

/* ===== RESPONSIVE DESIGN ===== */

/* Large tablets and small desktops */
@media (max-width: 992px) {
    .failure-card {
        padding: 50px 60px;
        max-width: 600px;
    }
    
    .failure-title {
        font-size: 2.2rem;
    }
}

/* Tablets */
@media (max-width: 768px) {
    .failure-container {
        padding: 40px 20px;
        min-height: 60vh;
    }
    
    .failure-container::before,
    .failure-container::after {
        display: none;
    }
    
    .failure-card {
        padding: 40px 32px;
        border-radius: 16px;
    }
    
    .failure-icon {
        width: 90px;
        height: 90px;
        margin-bottom: 28px;
    }
    
    .failure-icon i {
        font-size: 42px;
    }
    
    .failure-title {
        font-size: 1.9rem;
        margin-bottom: 14px;
    }
    
    .failure-message {
        font-size: 1rem;
        margin-bottom: 36px;
    }
    
    .button-group {
        gap: 12px;
    }
    
    .btn {
        padding: 14px 28px;
        font-size: 0.95rem;
    }
    
    .help-options {
        gap: 24px;
    }
}

/* Mobile landscape and smaller tablets */
@media (max-width: 576px) {
    .failure-container {
        padding: 30px 16px;
    }
    
    .failure-card {
        padding: 36px 24px;
    }
    
    .failure-icon {
        width: 80px;
        height: 80px;
        margin-bottom: 24px;
    }
    
    .failure-icon i {
        font-size: 36px;
    }
    
    .failure-title {
        font-size: 1.6rem;
    }
    
    .failure-message {
        font-size: 0.95rem;
        margin-bottom: 32px;
    }
    
    .order-id {
        display: block;
        margin-top: 12px;
    }
    
    .button-group {
        flex-direction: column;
        gap: 12px;
        width: 100%;
    }
    
    .btn {
        width: 100%;
        padding: 15px 24px;
    }
    
    .help-section {
        margin-top: 32px;
        padding-top: 28px;
    }
    
    .help-options {
        gap: 20px;
    }
    
    .help-item {
        min-width: 100px;
    }
    
    .info-box {
        margin-top: 24px;
        padding: 14px 16px;
    }
}

/* Extra small devices */
@media (max-width: 400px) {
    .failure-card {
        padding: 32px 20px;
    }
    
    .failure-title {
        font-size: 1.4rem;
    }
    
    .failure-message {
        font-size: 0.9rem;
    }
    
    .failure-icon {
        width: 70px;
        height: 70px;
    }
    
    .failure-icon i {
        font-size: 32px;
    }
    
    .btn {
        font-size: 0.9rem;
        padding: 14px 20px;
    }
    
    .help-options {
        flex-direction: column;
        gap: 16px;
    }
}

/* ===== ACCESSIBILITY ===== */
@media (prefers-reduced-motion: reduce) {
    *,
    *::before,
    *::after {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
    }
}

/* Focus styles for keyboard navigation */
.btn:focus-visible {
    outline: 3px solid #dc3545;
    outline-offset: 3px;
}

.help-link:focus-visible {
    outline: 2px solid #dc3545;
    outline-offset: 2px;
    border-radius: 4px;
}

/* ===== PRINT STYLES ===== */
@media print {
    .failure-container {
        min-height: auto;
        padding: 20px;
    }
    
    .failure-card {
        box-shadow: none;
        border: 1px solid #ddd;
    }
    
    .button-group {
        display: none;
    }
    
    .failure-icon::after {
        animation: none;
    }
    
    .help-section {
        page-break-before: avoid;
    }
}
    </style>
</head>

<body>
    <%- include('../partials/header', { showSearch: false }) %>

        <div class="failure-container">
            <div class="failure-card">
                <div class="failure-icon">
                    <i class="fas fa-times"></i>
                </div>
                <h1 class="failure-title">Payment Failed</h1>
                <p class="failure-message">
                    <% if (typeof message !=='undefined' ) { %>
                        <%= message %>
                            <% } else { %>
                                Oops! Something went wrong with your payment. Please try again or use a different
                                payment method.
                                <% } %>
                                    <br>
                                    <% if (typeof orderId !=='undefined' && orderId) { %>
                                        Order ID: <span class="order-id">
                                            <%= orderId %>
                                        </span>
                                        <% } %>
                </p>
                <div class="button-group">
                    <a href="/checkout/payment-options" class="btn btn-primary">Try Again</a>
                    <a href="/" class="btn btn-secondary">Go to Homepage</a>
                </div>
            </div>
        </div>

        <%- include('../partials/footer') %>
</body>

</html>