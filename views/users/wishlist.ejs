<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Wishlist - SeatWorld</title>
  <link rel="stylesheet" href="/css/users/wishlist.css">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
</head>

<body>
  <%- include('../partials/header', { user: user || {}, cartCount: cartCount || 0, showSearch: false }) %>

    <div class="wishlist-container">
      <div class="wishlist-header">
        <h2>Your Wishlist</h2>
        <p>Manage your favorite items and move them to your cart</p>
      </div>

      <div class="wishlist-grid" id="wishlist-content">
        <% if (wishlist && wishlist.length> 0) { %>
          <% wishlist.forEach(item=> { %>
            <div class="wishlist-item" data-id="<%= item._id %>">
              <div class="product-image-box">
                <img src="<%= item.image %>" alt="<%= item.name %>">
              </div>

              <div class="product-info-box">
                <h3 class="product-name">
                  <%= item.name %>
                </h3>
                <div class="product-meta">
                  <span><strong>Color:</strong>
                    <%= item.color || 'Default' %>
                  </span>
                  <span><strong>Stock:</strong>
                    <%= item.stock> 0 ? (item.stock + ' units') : 'Out of Stock' %>
                  </span>
                </div>
              </div>

              <div class="product-price-box">
                <span class="price-value">₹<%= item.price.toLocaleString('en-IN') %></span>
              </div>

              <div class="action-box">
                <button class="add-to-cart" onclick="addToCartFromWishlist('<%= item._id %>', this)" <%=item.stock <=0
                  ? 'disabled' : '' %>>
                  <%= item.stock <=0 ? 'Out of Stock' : 'Add To Cart' %>
                </button>
                <button class="remove-btn" onclick="removeDirectly('<%= item._id %>', this)"
                  title="Remove from wishlist">
                  <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                      d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
                  </svg>
                </button>
              </div>
            </div>
            <% }) %>
              <% } else { %>
                <div class="empty-wishlist">
                  <span class="empty-icon">❤️</span>
                  <h3>Your wishlist is empty</h3>
                  <p>Seems like you haven't added any items to your wishlist yet.</p>
                  <a href="/products" class="continue-shopping">Explore Products</a>
                </div>
                <% } %>
      </div>

      <div class="pagination-wrapper">
        <%- include('../partials/pagination', { pagination: pagination, query: '' }) %>
      </div>
    </div>

    <%- include('../partials/footer') %>
      <script src="/js/users/wishlist.js"></script>
</body>

</html>