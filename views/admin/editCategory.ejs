<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Category - Admin Panel</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons+Outlined" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="/js/admin/common.js"></script>
    <link rel="stylesheet" href="/css/admin/editCategory.css">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</head>

<body class="font-display bg-gray-50 text-gray-800">
    <!-- Top Header -->
    <%- include('../partials/adminHeader') %>

        <div class="flex pt-16 min-h-screen">
            <!-- Sidebar -->
            <%- include('../partials/adminSidebar') %>

                <!-- Overlay -->
                <div id="sidebarOverlay" class="fixed inset-0 bg-black/20 z-30 hidden md:hidden backdrop-blur-sm"></div>

                <!-- Main Content -->
                <main class="flex-1 p-8 overflow-y-auto bg-white/50">
                    <div class="max-w-4xl mx-auto">
                        <div class="form-container">
                            <div class="title text-orange-500">Edit Category</div>
                            <form action="/admin/edit-category/<%= category.id || category._id %>" method="POST"
                                id="editCategoryForm">
                                <div class="form-group">
                                    <label for="categoryName">Category Name</label>
                                    <input type="text" id="categoryName" name="categoryName"
                                        placeholder="Enter category name"
                                        value="<%= category ? (category.name || category.categoryName || '') : '' %>"
                                        required>
                                </div>
                                <button type="submit" class="submit-btn font-bold">Save Changes</button>
                            </form>
                        </div>
                    </div>
                </main>
        </div>

        <script type="module" src="/js/admin/editCategory.js"></script>
</body>

</html>